import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miapp.wallpaper',
  appName: 'wallpaper',
  webDir: 'dist/wallpaper'

};

export default config;
